name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify:
    name: Project Verification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Project-specific setup
        run: |
          echo "Replace this step with project-specific setup."
          echo "Examples: Node.js for React, Python for FastAPI, Java for Spring Boot."

      - name: Dependency installation
        run: |
          echo "Replace this step with dependency installation commands."

      - name: Verification commands
        run: |
          echo "Replace this step with project-specific verification commands."
          echo "Examples: tests, lint, typecheck, build, or framework checks."
